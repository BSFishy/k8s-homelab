apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: longhorn
spec:
  rules:
    - host: longhorn.home.mattprovost.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: longhorn-frontend
                port:
                  number: 80
