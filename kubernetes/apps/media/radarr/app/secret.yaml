---
apiVersion: generators.external-secrets.io/v1alpha1
kind: Password
metadata:
  name: radarr-secret
spec:
  length: 64
  noUpper: false
  allowRepeat: true
  symbols: 0
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: radarr-secret
spec:
  refreshPolicy: CreatedOnce
  target:
    name: radarr-secret
    creationPolicy: Owner
    template:
      type: Opaque
      data:
        RADARR__AUTH__APIKEY: "{{ .password }}"
  dataFrom:
    - sourceRef:
        generatorRef:
          apiVersion: generators.external-secrets.io/v1alpha1
          kind: Password
          name: radarr-secret
