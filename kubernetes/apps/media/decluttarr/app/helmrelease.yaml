---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: decluttarr
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  install:
    remediation:
      retries: -1
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  values:
    controllers:
      decluttarr:
        containers:
          app:
            image:
              repository: ghcr.io/manimatter/decluttarr
              tag: v1.50.2@sha256:8a00eaae4331327923513f4e26fda3f130e99d8a26c8e7bc9843f8dc56774a19

            env:
              - name: TZ
                value: America/Chicago
              - name: RADARR_API_KEY
                secretKeyRef:
                  name: "radarr-secret"
                  key: "RADARR__AUTH__APIKEY"
              - name: SONARR_API_KEY
                secretKeyRef:
                  name: "sonarr-secret"
                  key: "SONARR__AUTH__APIKEY"

            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: { drop: ["ALL"] }

            resources:
              requests:
                cpu: 100m
              limits:
                memory: 4Gi

    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch

    persistence:
      config-file:
        type: configMap
        name: "decluttarr-config"
        globalMounts:
          - path: /app/config/config.yaml
            subPath: config.yaml
