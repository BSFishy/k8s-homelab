---
apiVersion: generators.external-secrets.io/v1alpha1
kind: Password
metadata:
  name: jackett-secret
spec:
  length: 64
  noUpper: false
  allowRepeat: true
  symbols: 0
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: jackett-secret
spec:
  refreshPolicy: CreatedOnce
  target:
    name: jackett-secret
    creationPolicy: Owner
    template:
      type: Opaque
      data:
        JACKETT__APIKEY: "{{ .password }}"
  dataFrom:
    - sourceRef:
        generatorRef:
          apiVersion: generators.external-secrets.io/v1alpha1
          kind: Password
          name: jackett-secret
