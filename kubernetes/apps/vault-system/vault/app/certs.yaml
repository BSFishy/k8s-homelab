---
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: vault-ca-issuer
spec:
  ca:
    secretName: tls-ca
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vault-ca
spec:
  isCA: true
  commonName: Vault Local CA
  secretName: tls-ca
  issuerRef:
    name: selfsigned-root
    kind: ClusterIssuer
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vault-server
spec:
  secretName: tls-server
  dnsNames:
    - vault-0.vault-system
    - vault-1.vault-system
    - vault-2.vault-system
  issuerRef:
    name: vault-ca-issuer
    kind: Issuer
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vault-ingress
spec:
  secretName: tls-ingress
  commonName: "vault.home.mattprovost.dev"
  dnsNames:
    - "vault.home.mattprovost.dev"
  issuerRef:
    name: cluster-issuer
    kind: ClusterIssuer
  privateKey:
    algorithm: RSA
    size: 2048
